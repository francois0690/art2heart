<%= simple_form_for(@piece) do |f| %>
  <%= f.input :name %>
  <%= f.input :artist %>
  <%= f.input :category %>
  <%= f.input :price %>
  <%= f.input :photos, hint: "Votre oeuvre", label:'Selectionner une photo', as: :file, input_html: { accept: "image/*" }%>
  <%= f.submit %>
<% end %>
